# 0.1.0 integrated qualification

The qualification record of the re-frozen 0.1.0 candidate (Session 05-7,
2026-09-26): one candidate meant to close the seven release-blocking findings
of Sessions 05-1, 05-2 and 07 on the stock Tree-sitter 0.27.0 runtime, through a
pre-release schema re-freeze
([ADR-0007](../design/decisions/ADR-0007-pre-release-schema-refreeze.md)) and
an error-recovery external scanner
([ADR-0008](../design/decisions/ADR-0008-error-recovery-scanner.md)). It
supersedes the product identity of [0.1.0-release.md](0.1.0-release.md), whose
history stays valid for the requirements, fixtures and earlier fixes.

**Status (2026-09-26): released** as `v0.1.0` after the owner's approval
([0.1.0-release.md](0.1.0-release.md#release-v010)). Session
05-7-2 (below) confirmed all seven closures in three passing runs of the
release qualification lane (V10, this Windows host) after a disclosed
revision of the lane's cancellation check. The history follows. The first confirmation run of the release
qualification lane on the candidate (`lane-c1`, protocol v2) failed four
gates: MAX-CALLBACK-GAP and CLEANUP-ALL compare absolute times with fixed
bounds, A5-01-COST and VALID-PARSE compare cost ratios against a reference
and growth exponents. Environmental interference is suspected, not
established, and no product regression is identified (below); the failure
stays. Session 05-7-1 (below) corrected the CANCEL judgement and ran one
new confirmation campaign on the unchanged product; it failed in its first
run on the lane's actual-cancellation registration (below), and the
candidate stayed on hold until Session 05-7-2.

## Candidate identity

| Item | Value |
|---|---|
| Version | 0.1.0; `package.json` stays `private`; no package is published |
| Generator, ABI | tree-sitter 0.27.0 (ADR-0002), ABI 15 |
| Runtime measured | stock 0.27.0 (lane); support runs on 0.25.1 and 0.26.13 (crash and caps only) |
| Product commit | `87162a9` (branch `session/05-7-integrated-release-development`) |
| `grammar.js` | `511f47f7d4490c645253a1c12d0844c0a324e3c7782c5d96c2b2258f1e345c21` |
| `src/parser.c` | `9d6839b3bdf90956f627c2c8bbc4fa11f03769a24232d217494cb90299c177c3` (1,551 states, 198 symbols) |
| `src/scanner.c` (hand-written, ADR-0008) | `99791fb53880c8079f5d35fa9bcd66b99f7163db7e197899fe7d2fe028f09e2a` |
| `src/grammar.json` | `2df1b4b689996c53d1758d8a101ca0c1e7ab5caeb5854471b0cc26f2f37a21a2` |
| `src/node-types.json` | `642e6d2fb4792bf171daf7aabcd3208ea027105e312ea9289d5112d7efd56d15` (60 named node types; changed from H's `dfa5bfce…` by the re-freeze in `eea9be4`, unchanged between the Session 05-7 commits since) |
| `src/tree_sitter/*` | unchanged from 0.1.0 (`b29c1c9f…`, `20e6ba85…`, `180b893c…`) |
| `queries/highlights.scm` | `57e12ef346a7d62491fc98f69c4af3cd56c862cb2a1b80831711e39dcbdced43` |
| Level 1 snapshot | `roku-docs-2026-09-26` (content equal to `roku-docs-2026-09-23`; [upstream-sources.md](../provenance/upstream-sources.md)) |

Commits from `47d4047` (the ADR-0007 branch exception): `489ce4a` contract
(ADR-0007, ADR-0008); `eea9be4` grammar, scanner, query and fixtures;
`ec14a17`, `75708eb`, `23752ec` qualification lane and CI guards; `0f6e35b`
Level 1 refresh; `cc664de` first record; `c1088b5` recovery locality and the
end-of-input line end; `50183b8` lane corrections (review B); `86eac11`,
`b501847`, `e093ac8`, `4ff5633`, `494ecb2`, `d3764a3`, `7fdc052` scanner fixes
after the delta reviews below; `87162a9` review A7 corrections; then this
record (documentation only).

## What changed

- **Schema re-freeze** ([tree-schema.md](../specs/tree-schema.md) "Re-freeze
  of 0.1.0"): a called member is one `call_expression` with `object`,
  `property` and `arguments`; PRINT items are direct children of
  `print_statement` without an `expression` wrapper; six error-only named
  forms (`open_parenthesis`, `open_bracket`, `minus_sign`, `plus_sign`,
  `not_operator`, `try_keyword`) appear only inside `ERROR` nodes. Every
  valid public tree projects onto the earlier schema without loss
  (SEM-PUBLIC below).
- **Error-recovery scanner** (grammar-design §16, ADR-0008 decisions 2, 3
  and 5): in the runtime's error state only, the rest of a malformed line is
  one hidden token when it is long (16 units or more), or when it is short
  and neither a statement line, a closing bracket nor the end of input
  follows; the line end of a line with a long run is a recovery line end,
  valid only where a line or a block body may begin; at the end of input an
  empty line end is returned at most twice per stack version. It produces
  no token in a valid parse.
- **Stack and node reductions**: block headers as hidden rules,
  `_block_head` and `_body`; six inlined helpers.
- **Query**: sibling-order patterns for method calls and the error-only
  forms; W11 unchanged in meaning.

## Closure of the seven blocking findings

Before: the 0.1.0 release parser (`2711f7cc…`, H) as recorded in
[0.1.0-release.md](0.1.0-release.md#status-hold) and
[0.1.0-structural-recovery-safety.md](0.1.0-structural-recovery-safety.md).
After: lane run `lane-c1` on `87162a9` (stock 0.27.0, native probe) unless
marked otherwise.

| Finding | Before | After (`lane-c1`) | Live known-bad |
|---|---|---|---|
| B5-01 recovery memory | `x = +f([`×400: 146 MiB live, ×800: 512 MiB cap | PASS: allocator peak live ≤ 14 KB on every point, every consecutive pair exponent ≤ 0.60 | H fails the three CLI B5-01 rows |
| B5-02 stack overflow | `print ` + `,+*` or `f([)`×20,000, `x = -f(-)`×20,000 crash | PASS: complete; parse ≤ 1.71 ms, commit ≤ 1.9 MiB at 20,000; complete on 0.25.1 and 0.26.13 (50 of 50 runs) | H overflows the CLI stack at k = 16,000 |
| A5-01 PRINT query and navigation | 32,000 items: query 39 s | **not established**: A5-01-COST FAILED in `lane-c1` (navigation pair exponents 2.53, 1.83 and 1.63 on the A501J series; cost ratios ≤ 1.32); the diagnostic run `lane-d1` on the same commit passed it (ratios ≤ 1.28, exponents ≤ 1.47); the long-PRINT HTML is byte-identical to H | H fails the CLI PRINT query row |
| S07-M01 callback-free steps | 169 ms without a callback at the end of input; a 200 ms callback returned after 733 ms | **not established**: MAX-CALLBACK-GAP FAILED (`L-ANON-1MiB` 172.6 ms against the 100 ms bound); CANCEL PASS (return ≤ 200.4 ms, growth after the budget ≤ 14 KB) and CANCEL-OVERSHOOT PASS (≤ 66.3 ms); resume and reset equal fresh parses on all six registered inputs | protocol v3 record |
| S07-M02 large-input memory | B5-01 inputs reach the 512 MiB cap below 5 KB | PASS: every 1 MiB input ≤ 168.8 MiB commit, ≤ 2.08 s | protocol v3 record |
| S07-M03 query | `x = a` + `.b(1)[2]`×20,000 > 15 s; malformed `[`×20,000 quadratic | PASS: six malformed families, full-query exponent ≤ 1.14 (lane); twelve CLI query guards pass | H fails every method and malformed row |
| S08-M01 valid 1 MiB PRINT | 348 MiB, deletion 109–127 ms | memory PASS (168.8 MiB); deletion **not established**: CLEANUP-ALL FAILED (`V-PRINT-1MiB` 174.2 ms, `V-CALLS-1MiB` 130.2 ms, `V-LONGEXPR-1MiB` 144.4 ms against 100 ms) | Session 07 record |

KL-002 (quadratic-time recovery class) is retired with the same mechanism:
all 18 known families are linear through the CLI with and without a final
line break (local exponents ≤ 1.02 from k = 1,000 to 16,000), and the 81
KL-002-unit families of the lane's regression sweep stay ≤ 1.17 (all 297
families ≤ 1.40; [validation.md](../validation/validation.md) KL table).

## Release qualification lane

Protocol v2 (`confirmation-protocol-v2.json`, SHA-256
`ad6ea44d90ebf41d58a1c94e2c878e29779156d2c70cf534943f52f1168bfd8b`,
registered before any run on a commit after `cc664de`) requires three
complete runs on the final commit, one after another, every gate PASS in
each, no re-measurement, and a restart only after a fix.

| Run | Commit | Result |
|---|---|---|
| f1, f2, f3 | `ec14a17`, `75708eb`, `23752ec` (parser `8d206ca5`, superseded) | f1 failed three gates under operator load (harness event `LANE_CONCURRENT_OPERATOR_LOAD`); f2 failed CANCEL on a gate-logic defect since reverted; f3 passed its 16 gates. Not counted for the candidate. |
| t1–t4 | `eea9be4`, `c1088b5` | smoke runs of selected gates; not counted |
| **c1** | `87162a9`, clean tree | **FAIL** — 13 of 17 gates PASS; A5-01-COST, MAX-CALLBACK-GAP, CLEANUP-ALL and VALID-PARSE (ratios up to 1.81, pair exponents up to 1.42) FAIL. Runs c2 and c3 were not started (the driver stops at a failed run). Kept and disclosed. |
| d1 (diagnostic) | `87162a9` | the same gates: A5-01-COST PASS, MAX-CALLBACK-GAP 107.5 ms, CLEANUP-ALL 156.5 ms, VALID-PARSE FAIL |
| d2 (diagnostic) | the f3 identity (`23752ec`) | the same gates, minutes later: all PASS (52.1 ms, 74.6 ms) |
| va, vb (diagnostic) | `87162a9` lane with a no-op scanner / with the `cc664de` parser | CLEANUP-ALL 111.8 and 113.4 ms |

Diagnosis. Timed alternately on the same 1 MiB valid input outside the
lane (8 rounds), the four built probes delete their trees in 114.6–128.8 ms
(median) and parse in about 1.0–1.16 s, the f3 identity included, while the
f3 identity's own lane run had recorded 607 ms and 72.5 ms. After the runs
a game client, the anti-malware real-time scanner and a plugin process of
the user were present on the host with high accumulated CPU time; the
session does not control them. Process presence and accumulated CPU time
after a run are not a measurement of the load during it, and the A501J
NAV_CURSOR medians of `lane-c1` (k = 2,000, 4,000, 8,000: 0.59, 3.44 and
1.78 ms against 0.69, 2.60 and 1.54 ms for the reference) are irregular
rather than growing, which fits measurement noise but does not prove a
cause. No product change since `cc664de` is identified as the cause. The
diagnostic runs d1, d2, va and vb re-measured points after `lane-c1`,
which protocol v2 ("no point is re-measured within or after the runs") did
not allow; they are kept as diagnostics and are never counted as passes.
d2 ran a copy with the f3 files (parser `8d206ca5`) whose `identity.json`
names git HEAD `87162a9`, because git read the enclosing checkout; its HEAD
field alone does not identify it. Under the protocol `lane-c1` stays a
FAIL.

The other gates of `lane-c1`: RECOVERY-LOCALITY PASS (candidate hides rows
that H keeps in 23, 7 and 1 of the 1,699 mutants by ≥ 1, 5 and 20 rows; H
hides rows the candidate keeps in 86, 62 and 30); SEM-PUBLIC: the
projection of all 1,348 valid inputs equals H's (1,123 of them also with raw
trees equal to H's; the others differ only in the called-member shape
that the projection maps), error presence equal on all 1,463 error inputs, none incomplete,
and 404 of 404 comparator mutants detected; INCREMENTAL-REPAIR 30 of 30;
ABS-MEMORY ≤ 12.6 MiB;
RESUME-RESET triggered on all six inputs. In CANCEL every allocator run
that reached the budget crossed it at an allocation, and every cancelled
uninstrumented run at the progress callback; the allocator build's callback
crossing was not reached in a lane run (review B2-05; see Session 05-7-1).

## Recovery locality

Recovery trees are not contractual (grammar-contract §6). Beyond the lane
gate, independent reviewers measured the lines after an error with their own
generators (Session 05-7 reviews A2–A7, final scanner logic):

- A2's 374-case context × token matrix, with a clean, short or long
  malformed line before it: no declaration lost against H; more error rows
  than H in 2 cases, fewer in 2.
- 4,886 single-line mutants of six samples: the candidate loses
  declarations that H keeps in 9, H loses declarations the candidate keeps in
  93.
- 2,816 generated inputs that end inside open constructs: none lost against
  H; against `e093ac8`, 206 kept and 15 lost.
- 10,000 random end-of-input files: the candidate loses declarations that H
  keeps in 105, H in 3,260; two empty end-of-input line ends against one: 19
  kept, 5 lost.
- 9,990 byte prefixes of the samples: the root error status equals H's in
  both directions; declarations lost against H in 8 (22 when a lone `CR` and a
  statement follow), by H in 3,697.
- No input parses without an error where H reports one: about 60,000
  inputs in review A7 and 13,707 inputs scanned after review A4.

## Validation

| Level or gate | Result |
|---|---|
| V0, V1 (regeneration byte-identical twice, drift self-check), V2, V3, V4 (W11), V5 (W10), V10 (W06–W08, W13 fuzz 1,000 × 10 edits, seed 1) | PASS (local Windows, `87162a9`) |
| Recovery and query scaling guards (hosted subset: 21 recovery and 12 query rows) and 33 recovery goldens | PASS on the candidate; each row added or changed since `cc664de` fails live on its known-bad commit (`cc664de`, `47d4047`, `86eac11`, `b501847`, `e093ac8`; exponents 1.65–2.22) |
| W12 native oracle | recorded twice on `87162a9`, byte-identical: 231 cases (29 with error), content `08539e44dc7a9926112bd8b633161e8dca06239eb43fa5a22647602bb86fd552` |
| Release qualification lane | `lane-c1` FAIL (above); no complete confirmation |
| Highlight roles against H (2,817 inputs) | 1,354 valid inputs identical; the long-PRINT HTML byte-identical; error inputs below |
| Hosted CI (Windows, Ubuntu) | `87162a9`: run 36241734163 success; each earlier scanner commit also passed (`e093ac8` 36228028308, `4ff5633` 36230807058, `494ecb2` 36233699645, `d3764a3` 36236549280, `7fdc052` 36239598759) |
| Incremental parsing | `check_incremental.py` PASS; the reviewers' scripts, more than 2,000 targeted and random edits, equal fresh parses |
| V7 | not run (optional) |
| V8 | not run (no Roku device) |
| V9, W14 | not run (`go-treesitter` work is not authorized in this session) |

Highlight roles on inputs with errors (a change of meaning): 1,177 of 1,463
error inputs change; the change is mainly a loss of roles, not a count of 81
bytes. 75,640 of the changed bytes lie inside the candidate's `ERROR` node,
where a malformed line is one token without roles: 75,093 of them lose their
role, 522 change it and 25 gain one. 78 bytes in 24 inputs lie
inside H's `ERROR` node only: `end` from variable to keyword (69 bytes),
`function` from no role to keyword (8) and `f` from variable to function
(1), because recovery now ends at the line end and parses the next
statement. 3 bytes in one input lie outside both (`y = [` followed by 40
`-f(-)` and `end` on the next line): `end` is an `end_statement` of the
candidate (keyword) and an identifier of H (variable), outside every
`ERROR` and `MISSING` node of both trees; replacing the malformed line by
`y = []` as an incremental edit gives the fresh tree of the repaired source,
in which `end` is a keyword. None of the 81 bytes outside the candidate's
`ERROR` nodes lies at a `MISSING` node. Under DEV-571 the owner accepted
these changes on conditions (these counts and the END repair) that the
session checked; the acceptance, P571-HIGHLIGHT (below), covers exactly these
changes and no future one.

## Independent review

Each review ran in an isolated copy without write access to the product or
the remote; material findings were fixed, re-verified and reviewed again as a
delta. The reviewers were separate agent contexts of the same model family,
started by the same operator, not independent third parties. The reports are
kept with the session evidence.

| Review | Commit | Material findings | Disposition |
|---|---|---|---|
| A | `cc664de` | A-01 one malformed line could hide later blocks and subs | `c1088b5`: short rests token by token; lines-after-an-error goldens and `LOCALITY` |
| B (lane) | `cc664de`, `23752ec` | B-01 end-of-input recovery quadratic; B-02 CANCEL rule relaxed after registration; B-03 f3 against the noise policy; B-04 a sweep figure from no single run | B-01 `c1088b5`; B-02 reverted in `50183b8` (callback crossing); B-03 confirmation protocols v1 and v2; B-04 corrected here |
| A2 | `86eac11` | A2-01 `get_column` quadratic; A2-02 end-of-input quadratic after a closer or comment; A2-03 next line merged; A2-04 IF-header and bracket errors hid later subs | A2-01 `b501847`; A2-02–A2-04 `e093ac8`, reworked after A3 |
| B2 | `86eac11`, lane | B2-01 (= A2-01); B2-02 B5-01 pair exemption; B2-03 stale record | `b501847`; this record |
| A3 | `e093ac8` | A3-01 stale long-run flag; A3-02 end-of-input quadratic beyond a 256-character window; A3-03 whole-line run hid nested literals; A3-04 whole-file wraps | `4ff5633` |
| A4 | `4ff5633` | A4-01 a lone `CR` accepted as a line end in error-free parses; A4-02 flag kept after a keyword stop; A4-03 wrap claim | `494ecb2`; A4-03 withdrawn and measured (ADR-0008) |
| A5 | `494ecb2` | A5-05 truncated files wrapped whole (pre-existing, grammar stack shape) | disclosed (ADR-0008); A5-04 `d3764a3` |
| A6 | `d3764a3` | A6-01 second end-of-input token tied to keyword stops; A6-02 flag-lifetime claim | `7fdc052`; A6-01's cost disclosed |
| A7 | `7fdc052` | none | wording and counts `87162a9` |

## Known limitations and disclosures

- A long malformed line becomes one `ERROR` node; the tokens after the error
  point on that line are no longer separate nodes (highlight roles above).
- The recovery-shape trade-offs of ADR-0008 Consequences: a long run covers
  in-line resume points (statement after `THEN`); a stop before an unusable
  block keyword can let the next line join the malformed statement; a short
  rest before a block keyword likewise; a closing bracket ends a literal
  early; the second end-of-input line end can also enter an open construct;
  a `REM` comment after a long run on the same line is part of the run.
- Below more than 16 unclosed constructs the runtime finds no state that
  accepts the line end, and recovery may absorb lines that H keeps.
- A file cut off inside deeply nested constructs can be wrapped whole at the
  end of input (grammar stack shape; the no-op scanner gives the same trees).
- A lone `CR` stays unresolved (BS-LEX-007) and yields `ERROR`.
- RECOVERY-LOCALITY covers single-line mutants that end with a line break;
  end-of-input and multi-error behaviour is measured off-lane by the
  reviewers (B2-04), and its thresholds compare against H only.
- The lane runs on the local Windows host; hosted CI runs its CLI subset. The
  support runtimes are checked for completion within the caps only.
- Commit messages: the bodies of `86eac11` and `b501847` cite scanner hashes
  typed by hand (corrected in `e093ac8`); `494ecb2` says 32 guards (33) and
  `d3764a3` "22 with a lone CR tail" (22 with a lone `CR` and a statement);
  both corrected in the next commit bodies.
- Earlier limitations and requirement statuses are unchanged
  ([0.1.0-release.md](0.1.0-release.md#known-limitations)), except KL-002.

## Session 05-7-1: judgement correction and one confirmation campaign

The owner's instruction DEV-571 (2026-09-26) keeps the product frozen
(`87162a9`, the 13 components of K
`cfe6a1bcad3c8fd3d7bf1d42bacedac7c1fbca770990eb659a91eabb8342ad35`) and
decided:

- **P571-CANCEL.** `23752ec`'s growth-0 rule stays reverted (`50183b8`). The
  CANCEL judgement of `87162a9` read the reach of the budget from crossings
  and cancel flags only, so a run that took 250 ms of a 200 ms budget
  without a crossing could pass (a synthetic counterexample; `lane-c1` had
  no such run: six points finished before the budget, seven crossed it at an
  allocation). The corrected judgement judges each run by its own records
  (validation.md, CANCEL); re-judged from its raw records, `lane-c1`'s CANCEL
  keeps all 13 point verdicts. The allocator build's callback crossing,
  not reached in a lane run, was exercised with the unchanged probe source
  under a fake clock in a test-only build. The growth after the budget
  counts from the first allocation or callback after it, so the bytes of
  that first allocation are not in it; the allocator run's overall peak
  bounds it (`lane-c1`: at most 58,353,480 bytes, 55.7 MiB, on the seven
  cancelled inputs).
- **P571-F1.** f1 stays FAIL (A5-01-COST, CLEANUP-ALL, VALID-PARSE on
  `ec14a17`) and is excluded from the time qualification for the recorded
  one-worker breach (downloads and a recursive search by the session during
  its timed runs, harness event `LANE_CONCURRENT_OPERATOR_LOAD`).
- **P571-RECONFIRM.** One new campaign on the unchanged product, with the
  bounds, inputs, sample counts, floors and references of the gates
  unchanged: at most three complete lane runs, one after another, all 17
  result gates PASS in each, ending at the first run that is not a verified
  PASS, never restarted. `lane-c1` and the diagnostic runs stay recorded and
  are not among the three.
- **P571-HIGHLIGHT.** The error-input highlight changes above are accepted
  exactly as counted, bounded to this product and query; the conditions were
  checked by the session, not by the owner.

### Campaign S571-CONFIRM-01: failed in its first run

Protocol `S571-CONFIRM-01` (SHA-256 `08d964a0…`) and the qualification lock
(`f0e0da6b…`) fixed, before any run, the measured commit `15444151` (the
product of `87162a9`, the corrected judgement and these documents), the
runner arguments, the compiler, the three runtimes, the probe and supervisor
images (built twice before the campaign; byte-identical to `lane-c1`'s), the
17 result gates and their point counts, and a controller that verifies them.
The owner confirmed a ready host (other applications closed, another agent
session idle, a file-sharing client paused); a read-only observation before
the first run showed a processor median of 4.6 %.

Run q1 (2026-09-26, 15:46–15:54 UTC) passed 16 of the 17 gates, among them
the four that `lane-c1` failed: callback gap at most 46.2 ms, deletion at
most 66.8 ms, A5-01 ratios at most 1.04 and exponents at most 1.09,
VALID-PARSE ratios at most 1.11 and pair exponents at most 1.08. It failed
CANCEL: two of the seven inputs registered in `ec14a17` as parsing longer
than 200 ms (`L-FOREACH-1MiB`, `L-ANON-1MiB`) parsed in 165–172 ms and
184–191 ms on this host and so could not be cancelled, which the registered
obligation requires. Their return, deletion and growth after the budget
stayed within the bounds; the earlier judgement of `87162a9` gives the same
two failures, so the correction above does not decide it. The natural
parse of every 1 MiB input took about twice as long in `lane-c1` (for
example `V-PRINT-1MiB` 1,039–1,456 ms against 529–536 ms), which fits the
suspected interference in `lane-c1` without establishing it. Recomputed
from q1's 8,861 recorded runs, the 13 run-based gates equal the recorded
results.

Under the protocol the campaign ended at q1: q2 and q3 were not run, and
the campaign is not restarted. The failure is a registration of the lane
that depends on host speed, not a product failure; changing the registered
inputs or the obligation, and any new campaign, need the owner's decision
(Gate POLICY-EXTRA). The seven closures therefore stay not established.

## Session 05-7-2: CANCEL policy revision and one confirmation campaign

The owner's instruction DEV-572 (2026-09-27) keeps the product frozen
(`87162a9`, K `cfe6a1bc…ad35`) and approved one policy revision, one
admission pilot and one new confirmation campaign; it does not approve the
release.

- **P572-SEP.** CANCEL separates its time bounds from actual cancellation.
  All 13 cases keep their 200 ms point as a SAFETY point (return ≤ budget +
  100 ms, deletion ≤ 100 ms, growth after the budget < 64 MiB, a reached
  budget without an observed growth fails as before). Actual cancellation
  (every measured run cancelled, the allocator run's growth measured) is
  required of the seven families at 200 ms for five and at new 100 ms
  points for `L-FOREACH-1MiB` and `L-ANON-1MiB`, which finish before 200 ms.
  The 100 ms value was chosen after S571 q1; this is a policy revision, not
  the earlier check, and S571 q1 stays FAIL. A parse that ends on its own
  must report the registered error state and the input length (the frozen
  probe reports no root span); records the probe cannot produce (a
  cancellation without a crossing, allocator counters in an uninstrumented
  run) fail (validation.md, CANCEL; `c20eebd`, `66e1769`).
- **Correction of the S571 account.** The S571 text above calls its failure
  host-speed dependent. That is not established: the registration
  (`ec14a17`) precedes nine grammar and scanner commits of the product.
- **Admission.** One pilot on `c20eebd` (91 parser runs, fixed plan):
  every ACTUAL family's natural parse exceeded its budget (minimum over
  budget 1.63–4.50), every ACTUAL point cancelled 5 of 5 with its growth
  measured, and the two SAFETY points of the moved families passed. Its
  samples are admission evidence only.
- **Review.** The session reviewed the policy, the judgement, the pilot and
  the controller itself and in a separate context of the same model family
  (not independent): no material finding; its minor findings tightened the
  judgement and its self-test (`66e1769`).

### Campaign S572-CONFIRM-01: three runs passed

Protocol `S572-CONFIRM-01` (SHA-256 `b37333cb…`) and the qualification lock
(`e937918e…`) fixed, before any run, the measured commit `66e1769` (the
product of `87162a9`, the revised judgement and its documents), the runner
arguments, the compiler, the three runtimes, the probe and supervisor images
(built twice before the campaign; byte-identical to S571's), the 17 result
gates with their point counts, the 15 CANCEL points, and a controller that
checks every CANCEL point's sub-results and its links to the runs of its own
round. The owner confirmed a ready host; a read-only observation before the
first run showed a processor median of 4.7 %.

Runs q1 (2026-09-26, 18:53–19:01 UTC), q2 (19:01–19:09) and q3
(19:09–19:17) each passed all 17 gates. Recomputed from each run's 8,875
recorded runs with the judgement of `66e1769`, the 13 run-based gates equal
the recorded results, and every run links to one supervisor record.

| Finding | Result in q1–q3 (maximum over the three runs) |
|---|---|
| B5-01 recovery memory | allocator peak live ≤ 14,040 bytes; pair exponents ≤ 0.60 |
| B5-02 stack overflow | complete; parse ≤ 2.21 ms up to k = 20,000, commit ≤ 1.91 MiB; 50 of 50 support runs per run complete |
| A5-01 PRINT query and navigation | cost ratios ≤ 1.25, exponents ≤ 1.46 (bound 1.5) |
| S07-M01 callback-free steps | callback gap ≤ 49.5 ms; CANCEL 15 of 15 points (return ≤ 205.7 ms at 200 ms, ≤ 100.1 ms at 100 ms); overshoot ≤ 0.22 ms; the six resume inputs triggered and equal fresh parses |
| S07-M02 large-input memory | every 1 MiB input ≤ 168.8 MiB commit, ≤ 0.94 s (the PRINT-separator input time-exempt as in v3) |
| S07-M03 query | malformed-query exponents ≤ 1.07 |
| S08-M01 valid 1 MiB PRINT | 168.8 MiB commit; deletion ≤ 69.6 ms on every input |

Disclosures:

- `L-ANON-1MiB` at 200 ms is at the edge of its budget: 3 (q2) and 1 (q3)
  of its 5 measured runs reached 200 ms; the point passed because its
  allocator run also crossed the budget (growth ≤ 1,048,448 bytes). Had the
  allocator run finished first, the kept rule would have failed the run;
  this risk was disclosed before the campaign.
- The gate's growth after the budget omits the crossing allocation; the
  allocator run's overall peak bounds it: at most 64,487,752 bytes (61.5
  MiB) on the seven ACTUAL points.
- The result holds for this Windows host, the registered inputs and the
  stock 0.27.0 runtime (0.25.1 and 0.26.13 for crash and caps only). It is
  no guarantee for other hosts. V8 (Roku devices) and V9 (`go-treesitter`)
  are not run, and the Go scanner port is separate work.
- `lane-c1`, S571 q1 and the diagnostic runs keep their recorded results;
  none is counted among the three runs.
