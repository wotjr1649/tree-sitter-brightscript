# 0.1.0 release audit

Independent audit of the 0.1.0 release candidate
([0.1.0-release-candidate.md](0.1.0-release-candidate.md)), its evidence
remediation, full reverification and promotion decision (Session 04,
2026-09-23 UTC). Nothing is tagged, published or released.

**Verdict: `PROMOTION_READY`** (see Verdict). It supersedes the candidate's
`RELEASE_CANDIDATE_READY_BUT_LOCAL_MERGE_BLOCKED` for promotion to `main`;
it does not authorize a tag, a GitHub Release or a package.

## Input and method

| Item | Value |
|---|---|
| Immutable audit target | `session/03-grammar-implementation` @ `6fecc90f145265bd721e85f9371bba06339062c8` (tree `bc6f012d0700e4d1a6288637f415e08df19722e8`); local, `origin` and a fresh clone from `origin` agree; `main` = `origin/main` = `36ad29e3aa147ee09066b23736e51ca941233808`; no tag, release or npm package |
| Independence | Fresh clones of the exact target; the Session 03 conclusions were read as claims to test. The host injected summaries of earlier sessions at session start; they were not used as evidence |
| Initial reviewers | Three isolated reviewers, each in its own clone of `6fecc90`, offline, writing their reports before any synthesis: A specification and source authority; B grammar, runtime and tree schema; C release, reproducibility and supply chain. Lead checks: identity, Level 1 hashes, full gate run, W12 repeat recording, upstream and CI facts |
| No edit before the audit was recorded | Reviewer reports 15:20–15:44Z; synthesis recorded 15:57:28Z; branch `session/04-independent-release-audit` created from `6fecc90` 15:57:34Z; first change committed 15:59:49Z |
| Evidence | Local and Git-ignored: `artifacts/session-04-independent-release-audit/` (reports, logs, recordings) and the preserved Pre-Session 04 forensic subset `artifacts/pre-session-04/2026-09-23-rc-readiness-and-recovery-forensics/` (with SHA-256 list; the forensic conclusions had not been written to a file and were treated as unverified leads) |

## Findings

No `PRODUCT_MATERIAL` finding. Classes: `EVIDENCE_MATERIAL`, `HARDENING`,
`MINOR`, `INFORMATIONAL`; "fixed" means fixed in Session 04, "deferred" means
left to a later product session because the change would touch requirement
rows, fixtures or the grammar.

| ID | Class | Finding | Disposition |
|---|---|---|---|
| S04-E1 | EVIDENCE_MATERIAL | The RC report said the C runtime 0.25.1 and the CLI runtime 0.27.0 produce different recovery trees. They produce the same complete trees for all 227 W12 inputs and all steps of the 18 edit scripts (only the parse state of four `MISSING` leaves differs); both equal the CLI `--cst` output and the `go-treesitter` 0.25.1 records. Verified twice (reviewer C with its own C driver; lead diff of the dumps) | fixed |
| S04-E2 | EVIDENCE_MATERIAL | The W12 manifest hash in the RC report cannot be reproduced: the recording held the CLI's per-file timing for the 29 error inputs, the date and the hash of the parser library the CLI compiles (it differs between compilations). The trees are reproducible (three new recordings and the Session 03 one agree after removing the timing) | fixed: deterministic recorder, W12 re-recorded |
| S04-H1 | HARDENING | Session gate "any `FAIL` means the gate is not met" was ambiguous for a voluntarily run, non-required level (V9) | fixed: [validation.md](../validation/validation.md) wording |
| S04-H2 | HARDENING | CI actions referenced by the moving tag `v7`; checkout persisted the job token | fixed: pinned by commit, `persist-credentials: false` |
| S04-H3 | HARDENING | `check_generated.py` ran the downloaded CLI before comparing its SHA-256 | fixed: compared first; a mismatching binary is not run |
| S04-H4 | HARDENING | Session 03 reviewers' original reports were not preserved | recorded; Session 04 reports kept as local evidence |
| S04-H5 | HARDENING | `--cst` output used for error states grows with tree depth squared, so a very deep valid input would time out | deferred: fails as a timeout, never as a false pass; current W06 inputs are unaffected |
| S04-M1 | MINOR | RC report: grammar sources "last changed in `ade38b3`" (actually `a731bff`, `032a5c9`) | fixed |
| S04-M2 | MINOR | RC report: "every content region equal" (the `release-notes` region hash changed; text equal) | fixed |
| S04-M3 | MINOR | RC report: all ten V9 differences attributed to five mechanisms (KR-0005: nine attributed, one not isolated); 34 of 39 edit steps incremental not stated | fixed; V9 stays FAIL |
| S04-M4 | MINOR | CI corpus step without private `TREE_SITTER_LIBDIR`/`TREE_SITTER_DIR` | fixed |
| S04-M5 | MINOR | validation.md under-described what CI runs; `_ref` reconciliations skipped in CI undocumented | fixed |
| S04-M6 | MINOR | RC report lacked the candidate-head CI run | fixed (35854647551, success) |
| S04-M7 | MINOR | Disclosure "`#end.` yields a local `ERROR`" inside a literal-false region: the `ERROR` can cover neighbouring region text | fixed: grammar-design §11 and RC report wording (recovery shape) |
| S04-M8 | MINOR | `catch true`/`false`/`invalid`/`LINE_NUM`, `#const x = invalid`/`LINE_NUM`, `#if invalid` parse without an error, the word being an `identifier` (grammar-design §4, BS-LEX-023); the BS-ERR-003 prose ("a literal") and Level 1 CC §Criteria (boolean constant values only) are broader than the enforced fixtures | disclosed in the RC report; a stricter grammar or registry wording is deferred |
| S04-D1 | MINOR | BS-COND-009 records no evidence although CC §Criteria gives the constant-name character set (`#const x$ = true` is accepted) | deferred |
| S04-D2 | MINOR | Leading or repeated PRINT separators and a bare `#error` are accepted without a registry row | deferred (non-contractual) |
| S04-D3 | MINOR | BS-COND-008 "a directive occupies its own line" is not enforced for code before a directive | deferred |
| S04-D4 | MINOR | BS-LEX-021 fixtures rely on tolerated and provisional forms without citing them | deferred |
| S04-D5 | MINOR | BS-LEX-025, BS-ERR-005 "always start their statements" does not hold in single-line branches (`if a then try = 1`) | deferred |
| S04-D6 | MINOR | Comma-first separators and a line break after an AA `:` give `ERROR` and have no registry row | deferred |
| S04-I1 | INFORMATIONAL | Evidence notes for BS-FUNC-004, BS-STMT-021 (Level 4 accepts), BS-LEX-012; reserved words as names in malformed input (BS-LEX-023); THEN-less `IF x ?.5` / `?[1]` | deferred |
| S04-I2 | INFORMATIONAL | A missing block terminator can leave later declarations inside one `ERROR` | disclosed in the RC report |
| S04-I3 | INFORMATIONAL | Runner images, Node 24.x (CI 24.20.0) and Python 3.x (CI 3.14.7) float; the CI npm version is not logged; the generator binary is hash-checked | recorded in upstream-sources |
| S04-I4 | INFORMATIONAL | W12 had been recorded at `12f44cd`, not at the candidate head | re-recorded at `a47312b`; the documentation-only head `ac73973` gives the same content SHA-256 (re-auditor R1) |
| S04-I5 | INFORMATIONAL | Downstream evidence exists only on the local `go-treesitter` branch `session/03-brightscript-integration` (`ad232fc`) | recorded; no downstream action |

Pre-Session 04 leads: F-1 confirmed (S04-E1); F-2 confirmed (S04-E2); F-3
confirmed as over-broad wording (S04-M3); F-4 confirmed (S04-H1); F-5
confirmed (S04-H2); F-6 confirmed (S04-H4); F-7 confirmed, not a product
blocker (S04-I5).

Upstream Tree-sitter (checked 2026-09-23 UTC): no stable release after
`v0.27.0` (npm `latest` 0.27.0); #5910 (runtime error recovery) and #5925
(keyword tokenization under lexical conflicts) are open. Materiality: **no
material impact**. The RC workload shows no 0.25.1/0.27.0 difference and
recovery shape is not contractual; 54 keyword-prefixed identifiers in 10 valid
positions each (540 statements) parse as single identifiers with no error;
reviewer B's 8,432-case matrix of contextual keywords used as names behaves as
documented except S04-D5; splitting of invalid input (`a modx`, `#iffy`) is
already disclosed.

## Remediation

| Commit | Change |
|---|---|
| `464824e` fix(evidence): make native oracle recording deterministic | `scripts/record_oracle.py` drops the timing, adds a content identity, moves date and compiled-library hash to `run.json`; W12 pass criterion in [workload-matrix.md](../validation/workload-matrix.md) |
| `8367dfb` ci: pin release-critical actions by immutable sha | `.github/workflows/ci.yml`; `scripts/check_generated.py` verifies before running; [upstream-sources.md](../provenance/upstream-sources.md) "Hosted CI actions" |
| `a47312b` docs: correct release evidence and gate semantics | RC report corrections and disclosures; validation.md session gate and CI scope; grammar-design §11 sentence |

No change to `grammar.js`, `src/`, `queries/`, `test/`, `tree-sitter.json`,
`package.json`, `package-lock.json`, the public schema or any requirement
status: `git diff 6fecc90 a47312b` touches only two scripts
(`record_oracle.py`, `check_generated.py`), the workflow and documentation.

## Candidate identity

| Item | Value |
|---|---|
| Commits | Last change outside documentation: `8367dfbf56fdf0e7b1c51a5f936f92df277823bb`. Reverified head: `a47312b5b2b7270851ddd783bca8674ec260ec18` (documentation), to which the fresh-clone gate run and the W12 recording below are bound; later commits change documentation only |
| Version, ABI, generator | 0.1.0; ABI 15; tree-sitter 0.27.0, binary windows-x64 `9fbc4f285c876b1a38c7e9d5223a51fb7842255285cdd7db3ffb0ba3934f2662`, linux-x64 `5a228811cdb3a01b7e4dd493c5fc5e05b0040a49ffede94e866c4c58ff2605db` (checked in CI) |
| Generated files | unchanged from the RC: `src/parser.c` `fec46d21914ccedb1242f068059262bd51130bb3e7b6aefb7acab9bb0d6c1d86`, `src/grammar.json` `e28ea6dfd918dbe9cb52dbdf7134c39a67035d8e44f9f0ba14b5938c8310e7d0`, `src/node-types.json` `dfa5bfce57b601ba24532f5ce1ddf8c3a1dd53a32b151262d9ef7a572fd6618d`, `src/tree_sitter/*` as in the RC report |
| Level 1 snapshot | `roku-docs-2026-09-23-r2`; all 20 recorded content-region hashes recomputed from the local snapshots |
| Requirements | 171: documented 97, provisional 25, tolerated 8, invalid 6, unresolved 23, out-of-scope 12; covered 136, none 35; no active `KL-NNN` |
| Fixtures | 224 in 13 corpus files: 195 positive, 13 negative, 16 recovery; the 210 of the Session 02 catalogue unchanged, 14 added in Session 03 with traceable reasons (`21dfeb3`, `a731bff`, `daa2095`); names unique; no `:skip`; 3 samples; 2 highlight files |
| Schema | 54 named node types (53 and `inactive_text`), supertypes `statement` and `expression`, 33 fields; equal to the catalogue |

## Validation of the candidate

Fresh clone of `a47312b`, fresh `npm ci`, Windows x64 (log
`10-reverification/gates-a47312b.log`):

| Level | v0.x gate | Result |
|---|---|---|
| V0 | required | PASS |
| V1 | required | PASS (regeneration twice byte-identical; binary verified before it runs) |
| V2 | required | PASS: W01, W02 224/224 |
| V3 | all implemented requirements | PASS: registry `--complete` (the `_ref` reconciliation, run in the working repository because a clone has no `_ref/`: inventory 102, ambiguities 35), W04, W05 11 pairs, W07, W09; positive fixtures and W03 samples free of `ERROR`/`MISSING` |
| V4 | `highlights.scm` | PASS: W11 98 + 21 assertions |
| V5 | representative edit set | PASS: W10 I01–I12, E1–E6 |
| V6 | required | PASS: W12 below |
| V7 | optional | NOT_RUN (optional) |
| V8 | per Level 2 policy | NOT_RUN (no Roku device; no campaign authorized) |
| V9 | only for integration claims | FAIL in Session 03, not rerun; no integration claim is made (Downstream) |
| V10 | pathological inputs and bounded fuzz | PASS: W06 17 inputs, W07, W08 29, W13 seeds, `tree-sitter fuzz` 1,000 iterations × ≤ 10 edits over 224 tests, seed 1 |

W12, deterministic: two recordings at `a47312b` give byte-identical
`inputs/`, `trees/`, `cst/` and `manifest.json` (only `run.json` differs):
227 inputs, 29 with an error, exactly the `:error` fixtures; workload SHA-256
`f925b333194f5014757a278e8dc4379f907a5406f2a04554dae366e42e9a736e`, content
SHA-256 `762aa190dc919dafc6921100769b9c813c21449149ba7d809e5907eb2ef7a9ac`,
manifest SHA-256 (win32)
`aa8f1df4350402597bdf9c7e305bae17e6592d046ad23743f4a8b6223df23c3f`. The trees
equal the Session 03 recording of `12f44cd` and a recording of `6fecc90` after
removing their timing.

## Hosted CI

Workflow `CI`, jobs `test (windows-latest)` and `test (ubuntu-latest)`:

| Commit | Run | Conclusion |
|---|---|---|
| `6fecc90` (audit target) | 35854647551 | success |
| `a47312b` (remediation; actions pinned by commit, the logs show the three pinned commits downloaded; linux-x64 binary `5a228811…` verified) | 35886862581 | success |
| `ac73973` (this report, first version) | 35887198054 | success |

## Known limitations and disclosures

No `KL-NNN` is active. Disclosed behaviour, provisional (25), tolerated (8) and
unresolved (23, not claimed) requirements are listed in the RC report "Known
limitations and disclosed behaviour", which now also covers S04-M7, S04-M8 and
S04-I2. Deferred findings: S04-H5, S04-D1–D6, S04-I1. Level 2 evidence: none.

## Downstream

V9 remains **FAIL** (Session 03, `go-treesitter` branch
`session/03-brightscript-integration` @ `ad232fc`, neither pushed nor merged;
KR-0005 ratchets ten recovery-tree differences, all on inputs with syntax
errors). Session 04 did not modify, rerun or reinterpret it. This repository
makes no `go-treesitter` compatibility claim; the downstream correction is a
separate task in that repository.

## Final re-audit

Two new isolated re-auditors, each in its own clone of `ac73973` taken from
`origin`, offline, neither the author of the remediation:

- R1, evidence, tooling and CI: S04-E2, S04-H2, S04-H3 and S04-M4 closed. W12
  recorded twice at `ac73973`: byte-identical manifests (only `run.json`
  differs), content SHA-256 `762aa190…`; a recording at `a47312b` reproduces
  the manifest SHA-256 above; the old recorder's output at `6fecc90` differs
  only in the removed timing fields. A substituted binary is not run by
  `check_generated.py` (the previous script ran it). Full gate set rerun
  (fuzz 1,000, seed 1): every result as above; the `_ref` reconciliation was
  not run in the clone. No change to product files.
- R2, documentation, contract and scope: S04-E1, S04-H1, S04-M1–M3, S04-M5–M8
  and S04-I2 closed; the new session-gate wording keeps every required gate
  required, lets no non-required `FAIL` support a claim and matches `AGENTS.md`;
  the classification (in particular S04-M8, S04-D1, S04-D3, S04-D5, S04-H5) is
  defensible and the deferrals are compatible with 0.1.0; no product, schema,
  fixture or status change; no `go-treesitter` compatibility claim.

Both agree that no `PRODUCT_MATERIAL` or `EVIDENCE_MATERIAL` finding is
unresolved. Their further findings were documentation precision and process
items, fixed in the commit that completes this report: "three scripts" (two),
the commit roles of `8367dfb` and `a47312b`, where the W12 recording is bound,
the `#end.` wording (the `ERROR` can cover region text on either side and the
`#end` of the closing line), the W12 row (timings are dropped, not kept), the
`#const` source of S04-M8, the CI npm version, the source of the `_ref`
reconciliation and the RC report heading "Evidence notes". Not changed:
standalone check scripts other than `check_generated.py` run the CLI without
their own hash check (R1-06, HARDENING, deferred: CI runs
`check_generated.py` before every step that runs the CLI, so no release
evidence comes from an unverified binary); informational notes on the W12
format and its date field (R1-07, R1-08).

## Verdict

`PROMOTION_READY`. Every Session 04 promotion condition holds: the frozen
candidate was audited before any change; no `PRODUCT_MATERIAL` or
`EVIDENCE_MATERIAL` finding remains; required hardening is done (S04-H5 and
R1-06 are deferred as unnecessary for 0.1.0, above); W12 is deterministic;
the reports are corrected; the session-gate wording is unambiguous; the CI
actions are pinned by commit; the full gate set passes locally and in hosted
CI; the re-audit passed. V9 remains `FAIL` and is not a gate for this
promotion (validation.md Gates table and session gate); the promotion rests on
that gate and on the owner's explicit authorization of this Session 04
promotion, not on any change to V9. Deferred items for the next product
session: S04-D1–D6, S04-H5, S04-I1 and the registry wording of S04-M8.

The commit that completes this report changes documentation only. Its hosted
CI run and that of the promoted `main` are recorded in the Session 04 handoff;
a failure of either withdraws this verdict.
